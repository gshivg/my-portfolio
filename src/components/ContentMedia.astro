---
import { Media } from 'accessible-astro-components'

const { imgSrc, reverseImg = false } = Astro.props
---
<script src="../js/scrollanimate.js"/>
<section class="my-64">
  <div class="container">
    <div class="grid grid-cols-1 gap-24 md:grid-cols-2">
      {!reverseImg ? <Media class="rounded-lg scroll-animation-blur" src={imgSrc} /> : ''}
      <div class="space-content flex flex-col justify-center hidden-item">
        <slot />
      </div>
      {reverseImg ? <Media class="rounded-lg scroll-animation-blur" src={imgSrc} /> : ''}
    </div>
  </div>
</section>
